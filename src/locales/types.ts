export default interface LocaleSchema {
  transaction: {
    title: string;
    createTransaction: string;
  };
  category: {
    title: string;
    createCategory: string;
  };
  settings: {
    title: string;
  };
}
